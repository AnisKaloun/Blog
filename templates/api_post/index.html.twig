{% extends 'base.html.twig' %}

{% block title %}HomePage{% endblock %}

{% block body %}
    {% for post in Posts %}
        <h1>{{ post.title }}</h1>
        <p>{{post.content}}</p>
    {% endfor %}
{% endblock %}
